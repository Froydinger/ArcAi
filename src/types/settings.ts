export interface Settings {
  theme?: 'light' | 'dark' | 'forest' | 'midnight' | 'ocean' | 'sunset' | 'purple'; // Added 'purple'
  instructions?: string;
  name?: string;
  avatarUrl?: string; // Added avatarUrl
}
